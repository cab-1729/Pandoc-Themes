\documentclass{article}
% Use upquote if available, for straight quotes in verbatim environments
\usepackage{upquote}
\usepackage[]{microtype}
\UseMicrotypeSet[protrusion]{basicmath} % disable protrusion for tt fonts
\makeatletter
\@ifundefined{KOMAClassName}{% if non-KOMA class
  \IfFileExists{parskip.sty}{%
    \usepackage{parskip}
  }{% else
    \setlength{\parindent}{0pt}
    \setlength{\parskip}{6pt plus 2pt minus 1pt}}
}{% if KOMA class
  \KOMAoptions{parskip=half}}
\makeatother
\usepackage{color}
\usepackage{fancyvrb}
\newcommand{\VerbBar}{|}
\usepackage{fvextra}
\DefineVerbatimEnvironment{Highlighting}{Verbatim}{breaklines,breakanywhere,commandchars=\\\{\}}
% Add ',fontsize=\small' for more characters per line
\usepackage{calc} % for calculating minipage widths
% Correct order of tables after \paragraph or \subparagraph
\usepackage{etoolbox}
\makeatletter
\makeatother
\usepackage{footnotehyper}
\usepackage{graphicx}
\makeatletter
\def\maxwidth{\ifdim\Gin@nat@width>\linewidth\linewidth\else\Gin@nat@width\fi}
\def\maxheight{\ifdim\Gin@nat@height>\textheight\textheight\else\Gin@nat@height\fi}
\makeatother
% Scale images if necessary, so that they will not overflow the page
% margins by default, and it is still possible to overwrite the defaults
% using explicit options in \includegraphics[width, height, ...]{}
\setkeys{Gin}{width=\maxwidth,height=\maxheight,keepaspectratio}
% Set default figure placement to htbp
\makeatletter
\def\fps@figure{htbp}
\makeatother
\usepackage[normalem]{ulem}
% Avoid problems with \sout in headers with hyperref
%\pdfstringdefDisableCommands{\renewcommand{\sout}{}}
\setlength{\emergencystretch}{3em} % prevent overfull lines
\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}
\setcounter{secnumdepth}{-\maxdimen} % remove section numbering
%---
\author{}
\date{}
\newlength{\ch}
\setlength{\ch}{\widthof{0}}
\usepackage[a4paper,textwidth=100\ch]{geometry}
%colors
\usepackage[table]{xcolor}
\definecolor{TextColor}{HTML}{1F1F1F}
\definecolor{CodeBgColor}{HTML}{F8F8F8}
\definecolor{PrimaryColor}{HTML}{306797}
\definecolor{AccentColor}{HTML}{696969}
\definecolor{LinkColor}{HTML}{2077B2}
\definecolor{BorderColor}{HTML}{DBDBDB}
\definecolor{SideBarBgColor}{HTML}{FAFAFA}
%links
\usepackage[colorlinks=true,allcolors=LinkColor,allbordercolors=LinkColor]{hyperref}
%metadata
\hypersetup{
	$if(subject)$
	$if(title-meta)$
	  pdftitle={$title-meta$},
	$endif$
	$if(author-meta)$
	  pdfauthor={$author-meta$},
	$endif$
	$if(subject)$
	  pdfsubject={$subject$},
	$endif$
	$if(keywords)$
	  pdfkeywords={$for(keywords)$$keywords$$sep$, $endfor$},
	$endif$
	$if(subject)$
	pdfsubject={$subject$},
	$endif$
	pdfsubject={$subject$},
	$endif$
	pdfcreator={XeLaTeX via Pandoc paradox-seraph theme},
}
	%-------underline all links
\makeatletter
\Hy@AtBeginDocument{%
	\def\@pdfborderstyle{/S/U/W 0.01}%eyeballing
}
\makeatother
	%-------https://tex.stackexchange.com/a/26085
%fonts
\usepackage{fontspec}
	%-------set global color
\makeatletter
	  \color{TextColor}\global\let\default@color\current@color
\makeatother
	%-------https://tex.stackexchange.com/a/29409
\setmainfont[Extension=.ttf,Path=/usr/share/fonts/pandoc-fonts/paradox-fonts/nunito-,UprightFont=regular,ItalicFont=italic,BoldFont=bold,BoldItalicFont=bolditalic]{}
\setmonofont[Extension=.ttf,Path=/usr/share/fonts/pandoc-fonts/paradox-fonts/,UprightFont=firacode]{}
\newfontfamily\headingfont[Extension=.ttf,Path=/usr/share/fonts/pandoc-fonts/paradox-fonts/robotoslab-,UprightFont=regular,BoldFont=bold]{}
\usepackage[fontsize=0.16666672in]{fontsize}
\linespread{1.1666}%1.5/1.2
%headings
\usepackage{sectsty}
\usepackage{titlesec}
\usepackage[most]{tcolorbox}
\titleformat{\section}{\fontsize{2.7em}{\baselineskip}\headingfont\color{black}\filcenter}{}{}{}
\let\Section\section
\renewcommand{\section}[1]{
	\begin{tcolorbox}[
		on line,
		notitle,
		interior hidden,
		sharp corners,
		top=3em,
		bottom=1.5em,
		boxsep=0em,
		boxrule=0.04166668in,
		colback=white,
		colframe=PrimaryColor
	]
	\Section{#1}
	\end{tcolorbox}
}
\titleformat{\subsection}{\fontsize{2.2em}{\baselineskip}\headingfont\color{black}\filcenter}{}{}{}[\textcolor{PrimaryColor}{\Rule[\baselineskip]{0.56\linewidth}{0.02083334in}}]%0.7*0.8
\titlespacing{\subsection}{0pt}{1.5em}{0em}
\renewcommand{\subsubsection}[1]{%complete overwrite of level 3 header might cause problems later
	\vspace{1.5em}
	\tcbox[
		on line,
		notitle,
		interior hidden,
		sharp corners,
		colback=white,
		coltext=black,
		frame hidden,
		enhanced,
		right=1em,
		left=1em,
		borderline east={0.02083334in}{0pt}{PrimaryColor},
		borderline west={0.02083334in}{0pt}{PrimaryColor},
		fontupper=\fontsize{1.7em}{\baselineskip}\headingfont
	]{#1}
	\vspace{0.5em}
}
\renewcommand{\paragraph}[1]{%complete overwrite of level 4 header might cause problems later
	\vspace{1em}
	\tcbox[
		on line,
		notitle,
		interior hidden,
		sharp corners,
		colback=white,
		coltext=black,
		frame hidden,
		enhanced,
		right=0em,
		left=0em,
		boxsep=0pt,
		top=0.010416667in,
		bottom=0.1em,
		borderline south={0.02083334in}{0pt}{PrimaryColor,dashed},
		fontupper=\fontsize{1.4em}{\baselineskip}\headingfont
	]{#1}
	\vspace{0.4em}
}
\titleformat{\subparagraph}{\fontsize{1.2em}{\baselineskip}\headingfont\color{black}}{}{}{}
\titlespacing{\paragraph}{0pt}{1em}{0.2em}
\raggedright
%quotes
\renewenvironment{quote}{
	\begin{tcolorbox}[
			notitle,
			frame hidden,
			interior hidden,
			sharp corners,
			enhanced,
			boxrule=0pt,
			colback=white,
			coltext=AccentColor,
			boxsep=0em,
			fontupper=\itshape,
			top=1em,
			bottom=1em,
			left=2.5\ch,
			borderline west={0.052083in}{0pt}{PrimaryColor}
	]
}{
	\end{tcolorbox}
	\color{TextColor}
}
%code background
\newenvironment{Shaded}{
	\begin{tcolorbox}[
		notitle,
		breakable,
		enhanced,
		colback=CodeBgColor,
		coltext=TextColor,
		colframe=BorderColor,
		arc=0.25em,
		outer arc=0.25em,
		boxrule=0.01041667in,
		boxsep=0em,
		left=0.125em,
		right=0.125em,
		fontupper=\fontsize{1em}{1.5em}
	]
}{
	\end{tcolorbox}
}
%syntax highlighting - taken from CodeMirror
\newcommand{\AlertTok}[1]{#1}
\newcommand{\AnnotationTok}[1]{#1}
\newcommand{\AttributeTok}[1]{\textcolor[HTML]{0000CC}{\FancyVerbBreakStart#1\FancyVerbBreakStop}}
\newcommand{\BaseNTok}[1]{#1}
\newcommand{\BuiltInTok}[1]{\textcolor[HTML]{3300aa}{\FancyVerbBreakStart#1\FancyVerbBreakStop}}
\newcommand{\CharTok}[1]{#1}
\newcommand{\CommentTok}[1]{\textcolor[HTML]{575757}{\FancyVerbBreakStart#1\FancyVerbBreakStop}}
\newcommand{\CommentVarTok}[1]{#1}
\newcommand{\ConstantTok}[1]{#1}
\newcommand{\ControlFlowTok}[1]{#1}
\newcommand{\DataTypeTok}[1]{\textcolor[HTML]{125D5F}{\FancyVerbBreakStart#1\FancyVerbBreakStop}}
\newcommand{\DecValTok}[1]{\textcolor[HTML]{5B1D68}{\FancyVerbBreakStart#1\FancyVerbBreakStop}}
\newcommand{\DocumentationTok}[1]{#1}
\newcommand{\ErrorTok}[1]{\textcolor[HTML]{FF0000}{\FancyVerbBreakStart#1\FancyVerbBreakStop}}
\newcommand{\ExtensionTok}[1]{#1}
\newcommand{\FloatTok}[1]{\textcolor[HTML]{5B1D68}{\FancyVerbBreakStart#1\FancyVerbBreakStop}}
\newcommand{\FunctionTok}[1]{\textcolor[HTML]{125D5F}{\FancyVerbBreakStart#1\FancyVerbBreakStop}}
\newcommand{\ImportTok}[1]{#1}
\newcommand{\InformationTok}[1]{#1}
\newcommand{\KeywordTok}[1]{\textcolor[HTML]{1675CE}{\FancyVerbBreakStart#1\FancyVerbBreakStop}}
\newcommand{\NormalTok}[1]{\textcolor[HTML]{125D5F}{\FancyVerbBreakStart#1\FancyVerbBreakStop}}
\newcommand{\OperatorTok}[1]{\textcolor[HTML]{173D61}{\FancyVerbBreakStart#1\FancyVerbBreakStop}}
\newcommand{\OtherTok}[1]{#1}
\newcommand{\PreprocessorTok}[1]{#1}
\newcommand{\RegionMarkerTok}[1]{#1}
\newcommand{\SpecialCharTok}[1]{#1}
\newcommand{\SpecialStringTok}[1]{#1}
\newcommand{\StringTok}[1]{#1}
\newcommand{\VariableTok}[1]{\textcolor[HTML]{125D5F}{\FancyVerbBreakStart#1\FancyVerbBreakStop}}
\newcommand{\VerbatimStringTok}[1]{#1}
\newcommand{\WarngingTok}[1]{#1}
%no highlighting code
\RecustomVerbatimEnvironment{verbatim}{Verbatim}{breaklines,breakanywhere}%https://tex.stackexchange.com/a/469504
\BeforeBeginEnvironment{verbatim}{\begin{Shaded}}
\AfterEndEnvironment{verbatim}{\end{Shaded}}
%inline code behaviour
\def\inlinecode #1 {
	\tcbox[
		 notitle,
		 on line,
		 coltext=TextColor,
		 colback=CodeBgColor,
		 colframe=BorderColor,
		 arc=0.25em,
		 outer arc=0.25em,
		 boxrule=0.01041667in,
		 boxsep=0em,
		 left=0.125em,
		 right=0.125em,
		 fontupper=\fontsize{0.15625005in}{1.4em}
	]{
	#1
	}
	\color{TextColor}
}
\newcommand{\VERB}{\inlinecode\Verb[breaklines,breakanywhere,commandchars=\\\{\}]}
\renewcommand{\texttt}[1]{\VERB|#1|}
%tables
	%underline on every row
\makeatletter
\let\Tabularcr\@arraycr
\def\@arraycr{\Tabularcr\hline}
\makeatother
	%https://stackoverflow.com/a/3175486 second comment
\usepackage{xstring}
\usepackage{longtable}
\let\Longtable\longtable
\let\EndLongtable\endlongtable
\def\table#1{\Longtable{#1}}
%deviation: not making table head bold and capital to give user more freedom
%deviation: not stretching table accross width for aesthetics
\setlength{\tabcolsep}{0.7em}
\renewcommand{\arraystretch}{1.5}%eyeballing
\arrayrulecolor{BorderColor}
\renewenvironment{longtable}[3]{
	\vspace{1em}
	\StrGobbleLeft{#3}{2}[\pream]
	\StrGobbleRight{\pream}{2}[\pream]
	\StrSubstitute{\pream}{l}{|l}[\pream]
	\StrSubstitute{\pream}{r}{|r}[\pream]
	\StrSubstitute{\pream}{c}{|c}[\pream]
	\edef\pream{{\pream|}}
	\newcommand{\toprule}{}
	\newcommand{\midrule}{}
	\def\endhead{}
	\newcommand{\bottomrule}{}
	\begin{center}
	\rowcolors{2}{SideBarBgColor}{white}%https://tex.stackexchange.com/a/5365
	\expandafter\table\pream
	\noalign{\global\arrayrulewidth=0.01041667in}%https://tex.stackexchange.com/a/337790
	\rowcolor{SideBarBgColor}
	\hline
}{
	\EndLongtable
	\end{center}
	\vspace{1em}
}
%lists
\AtBeginEnvironment{itemize}{
	\let\Item\item
	\renewcommand{\item}{\Item[\textcolor{PrimaryColor}\textbullet\hspace{1.8mm}]}%1.8 from eyeballing
}
\AtBeginEnvironment{enumerate}{
	\let\Tightlist\tightlist
	\renewcommand{\tightlist}{
		\def\labelenumi{\bfseries\color{PrimaryColor}\textbf{\arabic{enumi}.}}
		\Tightlist
	}
}
%horizontal rules
\let\Rule\rule
\renewcommand{\rule}[2]{\textcolor{PrimaryColor}{\Rule{\linewidth}{0.02083334in}}}
%document
\begin{document}
$body$
\end{document}
